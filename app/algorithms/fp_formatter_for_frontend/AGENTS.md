`fp_wall_formatter.txt` is the First Algorithm that implemented but did worked correctly.

`fp_wall_formatter.py` is the new implementation witch going to do in small step, validation each step before moving to next step to avoid errors.

`debug.py` will use to quickly run the `fp_wall_formatter.py` function for quick testing temporary.
